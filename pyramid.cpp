#include <iostream>
using namespace std;

int main () 
{
    int rows i j spaces;
    cout << "Enter number rows: " ;
    cin >> rows;
    for (i = i, i <= rows; i++)
    {
        for (spaces = i; spaces < rows; spaces++)
        {
            cout << " ";
        }
        for (j =i; j<= 2 (2 * i-i); j++ )
        {
            cout << " * "
        }
        cout << " \n ";
    }
        return 0;
}